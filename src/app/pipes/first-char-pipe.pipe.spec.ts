import { FirstCharPipePipe } from './first-char-pipe.pipe';

describe('FirstCharPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCharPipePipe();
    expect(pipe).toBeTruthy();
  });
});
